# Cors
allow_cors_origins = ["https://backendery.io", "https://*.backendery.io", "http://localhost"]

# Email's
from_mailbox = "Backendery <hey@backendery.io>"
to_mailbox = "Backendery <hey@backendery.io>"

# Retry
retry_count = 3
retry_timeout = 50

# Smpt
smtp_connection_timeout = 5000
